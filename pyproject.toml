[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "qwrapper"
version = "0.1.1"
description = "Qwrapper"
authors = ["Daniel Dybing <danieldy@uia.no>"]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]
